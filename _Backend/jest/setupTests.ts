// console.log('jest/setupTests');

// Add any global test setup here
// For example, if you need to mock global objects or set up test environment

// Setup global mocks or Jest extensions
// For example:
// global.fetch = jest.fn();

// Extend Jest matchers (if needed)
// expect.extend({
//   yourCustomMatcher(received, expected) {
//     // Your logic here
//   }
// });

// No need to import @types/jest directly
// Jest types are already available through tsconfig.json
